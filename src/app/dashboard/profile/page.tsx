// Profile route page — renders the client ProfilePage component.

import { ProfilePage } from "@/components/profile/profile-page";

export default function ProfileRoutePage() {
  return <ProfilePage />;
}
